every real world websites does two things before giving the response to get request authentication and input validation
ANs middleware
suppose now if i want to do kidney replacement I have to again do input validaiton and kidney replacement 
It violates the DRY principle
Writing this again and again for every route is not Feasible 
to avoid DRY we use functions
now if I create functions to do checkups It will be slightly clear code but we will face again the code repitations 
We will use middleware 
Question is how u define a middleware If validaitons are right how it sends controls to the next middleware
So the thing is we can give a range of callback function in app.get function
the callback function will work as a chain in the callback the callback will have three parameters req res and next.
but We can not call res.send or res.json more then one time for a particular http request
Some of the popular middleware is calculateRequestsMiddleware
If we need post method's Body parameters use app.use
If I know that the middleware needs to be called in every route then I can use use method
 app.use(middlewareName)
for selfMade function we use app.use(middlewareName) not used ()
but for express.json we use () because it itself returns a function
 app.use(express.json())
 //Now we will learn about the input validaiton
 you should not expose any backend logic to user when there is an error in the backend So we have globalCathes which is another middleware which is called at the end of all the routers
 //*************authentication******
 till now anyone can send request to your backend 
 Dumb way : Ask the user to send request with header of username and password in all subsequent request 
 slightly better way :
 Give the user back a token on signup and signin
 ask the user to send back the token in alll future request
 when the user logs out ask the user to forget token(or revoke it from the backend)
 ///////////week3.2
 Till now we were sending request through postman or out browser
 When you hit a button in a website it sends a request to the server it is done by  the function fetch which is given by browser api
 FETCH.
 you can take your authentication token and go jwt.io
 Working with jwt tokens

 //Databases
 with momgoose ->noSql Database
 4 types :
 Vector DB ->used for machine learning
 Graph ->some niche usecase
 SQL ->Full stack website
 NOSql -> Simpler to understand //you can dump whatever data u want